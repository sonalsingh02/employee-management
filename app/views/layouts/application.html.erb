<!DOCTYPE html>
<html>
  <head>
    <title>EmployeeManagement</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  	<p class="navbar-text pull-right">
	<% if employee_signed_in? %>
  		Logged in as <strong><%= current_employee.email %></strong>.
  		<%= link_to 'Edit Account', edit_employee_registration_path, :class => 'navbar-link' %> |
  		<%= link_to "Logout", destroy_employee_session_path, method: :delete, :class => 'navbar-link'  %>
	<% else %>
  		<%= link_to "Sign up", new_employee_registration_path, :class => 'navbar-link'  %> |
  		<%= link_to "Login", new_employee_session_path, :class => 'navbar-link'  %>
	<% end %>
	</p>
  	<% if notice %>
  		<p class="alert alert-success"><%= notice %></p>
	<% end %>
	<% if alert %>
  		<p class="alert alert-danger"><%= alert %></p>
	<% end %>
    <%= yield %>
  </body>
</html>
